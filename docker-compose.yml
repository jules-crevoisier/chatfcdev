services:
  chatfc:
    build: .
    container_name: chatfc
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - chatfc_data:/app/data
      - chatfc_uploads:/app/uploads
    environment:
      PORT: "3000"

volumes:
  chatfc_data:
  chatfc_uploads:
